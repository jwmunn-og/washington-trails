<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Take a Hike" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <link href="https://fonts.googleapis.com/css?family=Roboto+Slab:700|Roboto:400,400i,700" rel="stylesheet">
  </head>

  <body>
    <div class="top-bar">
      <div class="container">
        <div class="top-bar-left">
          <ul class="menu">
            <li class="menu logo"><%= link_to 'Take a Hike', root_path %></li>
          </ul>
        </div>
        <div class="top-bar-right">
          <ul class="menu">
            <li><%= link_to 'Add a Trail', new_trail_path, class: 'nav-link' %></li>
          </ul>
        </div>
      </div>
    </div>
    <br />
    <%= yield %>

  </body>
</html>
